<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <android.support.design.widget.CollapsingToolbarLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fitsSystemWindows="true"
            app:contentScrim="?attr/colorPrimary"
            style="@style/VisibleLayoutStyle"
            app:layout_scrollFlags="scroll|exitUntilCollapsed">

            <ImageView
                android:id="@+id/toolbar_image_container"
                android:layout_width="match_parent"
                android:layout_height="@dimen/appbar_image_dimen"
                android:layout_gravity="center"
                android:scaleType="fitXY"
                android:src="@mipmap/ic_launcher"
                android:visibility="visible"
                app:layout_collapseMode="parallax" />

            <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:layout_gravity="bottom"
                app:layout_collapseMode="pin">

                <EditText
                    android:id="@+id/search_et"
                    android:layout_width="280dp"
                    android:layout_height="32dp"
                    android:background="@drawable/rounded_edges_toolbar"
                    android:backgroundTint="@color/searchBarColor"
                    android:drawableEnd="@mipmap/search_24px"
                    android:gravity="center"
                    android:hint="@string/toolbar_search_prompt_button"
                    android:inputType="text"
                    android:onClick="onSearch"
                    android:paddingEnd="8dp"
                    android:paddingStart="8dp"
                    android:textAlignment="center"
                    android:textSize="18sp"
                    android:alpha=".75"/>

            </android.support.v7.widget.Toolbar>

            <View style="@style/DividerStyle"
                android:layout_gravity="bottom" />

        </android.support.design.widget.CollapsingToolbarLayout>

    </android.support.design.widget.AppBarLayout>

</merge>